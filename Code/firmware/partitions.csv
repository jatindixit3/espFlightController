# Name,    Type, SubType,  Offset,   Size,     Flags
# 8MB flash total. No OTA (not requested) - single factory app slot.
# 'blackbox' is a raw data partition the firmware writes flight logs to directly
# (no filesystem), read back via MSP blackbox-download commands.
nvs,        data, nvs,      0x9000,   0x6000,
phy_init,   data, phy,      0xf000,   0x1000,
factory,    app,  factory,  0x10000,  0x300000,
blackbox,   data, 0x40,     0x310000, 0x400000,
